{
  "name": "nibble-dev-dungeon",
  "build": { "dockerfile": "Dockerfile" },
  "remoteUser": "node",
  "workspaceFolder": "/work",
  "runArgs": ["--name", "dev_my_webgl_env"],
  "mounts": [
    "source=${localWorkspaceFolder},target=/work,type=bind,consistency=cached"
  ],
  "postCreateCommand": "bash -lc 'if [ -f package-lock.json ]; then npm ci; elif [ -f package.json ]; then npm install; fi'"
} 